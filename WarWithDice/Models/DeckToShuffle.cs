using WarWithDice.Controllers;
using WarWithDice.Models;
using System.Collections;
using System.Runtime.CompilerServices;

namespace WarWithDice.Models
{
    //I think that controllers can only work with one other controller? 
    //So I think that I need to have this controller work with the game controller
    //Then the shuffle controller will work with this controller?
    //Or I am completley off and am going about this the wrong way.
    //public static class DeckToShuffle
    //{
    //    public static void AddCards(IList<Card> deckToShuffle)
    //    {
    //        deckToShuffle.Add(new Card());

            
    //    }
    

    //    public static void DealCards<Card>(this IList<Card> deckToShuffle, List<Card> list1, List<Card> list2) 
    //    {
    //        foreach (Card card in deckToShuffle) 
    //        { 
    //            list1 = deckToShuffle.ToList();
    //            deckToShuffle.Remove(card);

    //            list2 = deckToShuffle.ToList();
    //            deckToShuffle.Remove(card);

    //        }
    //    }
    //}



}
